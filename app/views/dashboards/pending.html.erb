<p class="back white"><%= link_to "Back", dashboards_path %></p>
<br>
<br>


<div class="friendships-page">
<h1 style="margin-bottom: 5vh;">Your Pending Requests</h1>

<div class="profile-container">
<% @friends.each do |user| %>
<div class="profile-card">
<div class="profile-avatar">
    <p><%= image_tag(user.avatar_url) %></p>
</div>
<div class="profile-details">
  <p><%= "#{user.first_name} #{user.last_name}" %></p>
  <p class="friend-location"><%= user.location %></p>
</div>
  <div class="send-request">
  <%= link_to "✓", confirm_friendship_path(user), class: "confirm" %>
  <%= link_to "X", "#", class: "delete", style: "margin-left: 3vw; margin-right: 1vw;" %>
  </div>
  </div>
<% end %>
</div>
</div>
